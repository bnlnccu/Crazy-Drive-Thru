// Rescue Kit: In FoodItem.cs > OnMouseDown(), REPLACE the single line "Destroy(gameObject);" with this block
// This is TODO F-3: stop conveyor + play toss animation + delayed destroy

var move = GetComponent<MoveToTarget2D>();
if (move != null) move.enabled = false;

if (anim != null)
{
    anim.enabled = true;
    anim.SetTrigger("Toss");
}
isBeingDestroyed = true;
Destroy(gameObject, 0.5f);
